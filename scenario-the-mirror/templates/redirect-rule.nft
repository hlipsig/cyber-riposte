# Auto-generated by cyber-riposte agent (The Mirror)
# Redirect attacker to honeypot for TTP collection while OSINT runs
# Attacker: {{ attacker_ip }}
# Honeypot: {{ honeypot_ip }}
# Trigger:  {{ trigger_signature }}

table ip nat {
    chain prerouting {
        type nat hook prerouting priority -100; policy accept;

        ip saddr {{ attacker_ip }} dnat to {{ honeypot_ip }} \
            comment "mirror: redirect to honeypot {{ timestamp }}"
    }
}
