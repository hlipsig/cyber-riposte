# Auto-generated by cyber-riposte agent
# Trigger: DNS queries to C2-associated domain
# Domain:  {{ domain }}
# Intel:   {{ intel_source }} ({{ intel_ref }})
# Queried by: {{ querying_hosts }}

server:
    local-zone: "{{ domain }}" redirect
    local-data: "{{ domain }} A {{ sinkhole_ip }}"
